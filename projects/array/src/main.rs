fn main() {
    //Rust中数组的语法
    let a = [1, 2, 3, 4, 5];
    
    let a:[i32;5] = [1, 2, 3, 4, 5];//[类型:元素数量]
    let a:[3;5];//[初始值:元素数量]=>[3, 3, 3, 3, 3]

    //访问元素数量
    let first = a[0];
    let second = a[1];

    //Rust安全原则，当索引超出数组的长度时，编译时不会报错。
    //但在运行时，会产生panic
}
